create a .env file to define email varaibles 

# SMTP / email settings for Hostinger (Titan)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Mail sender info used by the Flask app
SENDER_EMAIL=d
SENDER_NAME=

# Optional: address to receive test emails from smtp_test.py
TEST_TO=
